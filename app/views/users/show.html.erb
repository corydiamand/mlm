<% provide(:title, "#{@user.first_name} #{@user.last_name}") %>
<h2><%= @user.last_name %>, <%= @user.first_name %></h2>

<% if @statements.any? %>
  <%= render 'statements' %>
<% else %>
  <div id='no_statement'>No statement data available</div>
<% end %>